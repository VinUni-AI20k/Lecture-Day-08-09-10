# test/

Standalone test runners (not part of official scorecards).

- `run_hybrid_rerank.py`: run RAG with `retrieval_mode="hybrid"` + `use_rerank=True` and save outputs to `logs/test/` and `results/test/`.

